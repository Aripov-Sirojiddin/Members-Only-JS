<% if (currentUser) {%>
  <%- include("../partials/header.ejs", {isMember: isMember}) %>
  
  <% messages.forEach((message) => { %>
    <%- include("../partials/message.ejs", {message: message}) %>
  <% }); %>
  
<% } else { %>
  <%- include("../partials/login.ejs") %> 
<% }; %>